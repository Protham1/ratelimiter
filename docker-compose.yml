version: "3.8"

services:
  redis:
    image: redis:alpine
    ports:
      - "6380:6379"
    restart: always

  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: always
